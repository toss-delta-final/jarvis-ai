# PROPOSAL-I1-DIET-395 — I-1 응답 비대화, BE 협의 3건 제안서

이슈 #395. 운영에서 구매자 추천이 `SEARCH_FAILED`로 떨어졌다 — 필터 없는 I-1 검색이
**7.74초·12.3MB**(AI→Spring 3초 예산 초과)였다. AI 쪽 방어(#132·#393 등)는 이미 끝났고, 이 문서는
**BE와 협의할 3건을 "BE가 바로 결정할 수 있는 제안서"로 만든 것**이다. 산출물은 문서 1(본 문서) +
재현 스크립트(`scripts/measure_i1_field_bytes_395.py`) + 회귀 테스트(`tests/unit/
test_i1_field_diet_395.py`) + CHANGELOG다. `app/` 프로덕션 코드는 건드리지 않았다.

> **후속 실측 정본 안내**: 협의 3(rating/reviewCount, §6·§8·§9)의 BE 최종 결론은 캐싱이 아니라
> 커버링 인덱스(PR #133)였고, 그 효과를 운영 백업으로 실측한 결과는 이 문서가 아니라
> **`docs/specs/MEASURE-REVIEW-INDEX-395.md`가 정본**이다. 이 문서를 먼저 읽으면 캐싱 기록이
> 앞서 나와 오해할 수 있어 여기 미리 적어 둔다.

## §0 요약

1. I-1 응답의 **81.9%가 `attributes`**이고, 그중 **53.2%(전체의 43.5%)가 AI가 전혀 읽지 않는
   `_extra`**(리뷰 문장·시각 묘사문)다. `attributes` 내부의 `_extra`·`_source_pid`·`_domain`·
   `_category` 4키를 빼면 항목당 바이트가 **46.3~49.8% 준다**(실측 두 데이터소스 교차 확인, §3
   — 두 소스 다 `options` 컬럼이 없어 아래 `options`·`optionCount` 유지 결정은 이 수치에 영향을
   주지 않는다). `summary`·`options`·`optionCount`는 전부 미소비가 확인됐지만 최종 요청 대상에서
   뺐다(유지) — `summary`는 AI 쪽에 사본이 없어서(I-17이 주지 않는다, 2026-08-07 결정),
   `options`·`optionCount`는 소비 계획이 살아 있어서다(#455, 2026-08-07 확정, §5).
2. I-1은 결과 수 상한이 없다 — 매칭이 7,245건이면 7,245건을 전량 반환한다(`docs/api-spec.md`
   §4.6의 「[2026-07-23, BE 합의] `size` 제거 — 라운드1 전량 반환」 노트로 AI가 합의한 사양,
   `MEASURE-I1-RESPONSE-132.md` §8 처방이 그 사양을 유지하기로 재확인). 상한을 다시 두려면
   **정렬 기준**과 **`totalCount`**가 반드시 같이 와야 한다.
   그렇지 않으면 임의 표본 절단이거나(품질 붕괴), AI가 "그런 상품이 없다"고 조용히 오판한다(§4).
3. `rating`·`reviewCount`는 검색마다 `Review` 조인+집계로 계산된다(BE 주석 자인). BE에 **이미 있는**
   id-IN 배치 집계 경로(`ReviewService#getStats(ids)`, I-3/인기상품이 쓴다)가 있어, size 상한이
   생기면 그 경로로 갈아탈 여지가 열린다(§6).

| 협의 항목 | 제안 | 근거 한 줄 | BE 작업 크기(AI 추정) | BE 회신(2026-08-07) |
|---|---|---|---|---|
| 1. `size` 상한 (❌ 폐지, 2026-08-07) | 폐지 — BE에 요청하지 않으며 재개 계획도 없다 | 절단된 상품은 그 턴의 후보에 아예 들어오지 못하고, 결정적 정렬은 재현성만 줄 뿐 관련도를 보장하지 않는다 — 그 손실을 감수할 이유가 없다고 판단(역산·`ORDER BY` 부재·`totalCount` 논증은 폐지 근거로 §4에 보존, 정본도 같은 결론) | 해당 없음 | 해당 없음(송부 목록에서 제외) |
| 2. 필드 축소 | `attributes` 내부 `_extra`·`_source_pid`·`_domain`·`_category` 4키 제외 (`summary`·`options`·`optionCount`는 전부 유지) | 항목당 바이트 **46.3~49.8%** 감소(§3·§5) + `keyword` LIKE 오탐 감소(§5) | 소(직렬화 코드에서 필드 몇 개 제외) | **4키 전부 수용** |
| 3. rating/reviewCount 비정규화 | 근본: 리뷰 쓰기 시 집계 컬럼 갱신 / 중간: size 상한 도입 후 id-IN 배치 집계로 전환 | 검색마다 `LEFT JOIN Review ... GROUP BY` 도는 구조(§6) | 중(근본)~소(중간, 기존 코드 재사용) | **최종 결론(2026-08-08): 캐싱 철회, `idx_review_product`를 `(product_id, status, rating)` 커버링으로 확장**(BE PR #133). 실측으로 무필터 최악 3.8배 감소 확인(§6·`MEASURE-REVIEW-INDEX-395.md`) |

## §1 방법과 데이터 출처

무엇을 무엇으로 쟀는지:

- **§3 바이트 표**: repo 내 픽스처 `evals/goldenset/fixtures/catalog_snapshot.json`(6,585건)과
  BE 시드 SQL 덤프 `~/inte-final/_sql/mariadb/30_product.sql`(6,559건, repo 밖 자산) 둘 다로
  `scripts/measure_i1_field_bytes_395.py`를 돌려 교차 확인했다.
- **§2 소비 표**: 이 저장소(`app/`)의 정적 코드 인용 — 실행하지 않고 grep + 코드 읽기로 확인했다.
- **§4~§6 BE 근거**: BE 로컬 체크아웃 `~/inte-final/jarvis-backend`를 읽기 전용으로 열어
  `git show origin/main:<경로>`로 확인했다(작업 트리는 `742ba77`, 2026-07-31로 낡아 그대로 열면
  일주일 전 코드를 인용하게 된다).

재현 명령 한 줄:

```bash
uv run python scripts/measure_i1_field_bytes_395.py
```

측정 시점 **2026-08-07**. 근거 커밋 AI `4bba354`(`NyongCho/docs-395-i1-response-diet`, base `dev`),
BE `origin/main 4c6b287`(2026-08-07).

> ⚠️ 이 문서의 수치는 **측정 시점 기록**이다. 카탈로그·코드가 바뀌면 값이 달라진다 — 재측정 없이
> 현재 동작의 근거로 인용하지 말 것(`MEASURE-I1-RESPONSE-132.md` 말미와 같은 경고).

## §2 AI가 실제로 소비하는 필드

이슈 본문은 "AI 스키마가 파싱하는 건 11개 필드"라고 적었다. **그건 스키마 필드 수이지 소비 필드
수가 아니다** — 파싱은 하지만 아무도 읽지 않는 필드가 있다. 아래 표는 그 둘을 가른다. 소비 =
값을 실제로 읽어 판단·계산·프롬프트에 쓰는 것. 스키마 선언만으로는 소비가 아니다.

| 와이어 필드 | 파싱 | 소비 | 소비처(심볼) |
|---|:---:|:---:|---|
| `productId` | ○ | ○ | dedup 집합 구성(`app/agents/buyer/recommendation/graph.py` 다수), rerank 후보 키, I-21 push 대상 id |
| `name` | ○ | ○ | `app/agents/buyer/recommendation/rerank.py::rerank`(프롬프트 후보 dict 조립부, rerank 프롬프트 후보 항목) |
| `price` | ○ | ○ | `app/agents/buyer/recommendation/no_condition.py::within_budget`(예산 검증) · `rerank.py`의 `_price_tier`/`_price_medians`(가격 티어) |
| `rating` | ○ | ○ | `rerank.py::_rating_tier` · `search_service.apply_ai_side_filters`(평점 사후필터) · `app/agents/buyer/recommendation/graph.py::_unrated_product_ids`(무평점 판정) — **[정본 대조 각주]** 정본(Notion I-1)은 리뷰 0건이면 `rating: 0.0`으로 확정한다(I-17과 동일 규약) — 이 문서의 소비처 서술은 "null/미전송이면 rating이 지배"라 null과 0을 둘 다 상정한다. `docs/api-spec.md` 사본 동기화 대상, §10 후속 |
| `reviewCount` | ○ | ○ | `rerank.py::_review_tier` · `apply_ai_side_filters`의 rating=0 판별(리뷰 부재 vs 저평점 구분, #171) — **[정본 대조 각주]** 정본은 리뷰 0건이면 `reviewCount: 0`으로 확정한다(I-17과 동일 규약) — null과 0을 둘 다 상정하는 이 문서의 서술과 다르다. `docs/api-spec.md` 사본 동기화 대상, §10 후속 |
| `categoryName` | ○ | ○ | `rerank.py::rerank`(후보 dict 조립부, rerank 후보 `category`) · `graph.py`의 소모품 억제·그룹 키 |
| `brandName` | ○ | ○ | `rerank.py::rerank`(후보 dict 조립부, rerank 후보 `brand`) |
| `attributes` | ○ | **△** | `search_service._matches_attr_conditions`(명시 속성 하드매칭) **한 곳뿐**. rerank LLM 프롬프트에는 안 실린다(`rerank.py::rerank`의 후보 dict 조립부가 attributes를 프롬프트 dict에 넣지 않음). 내부 키 `_extra`(review_pros·review_cons·situation_tags·visual_features)는 I-1 와이어로는 미소비이지만 **AI DB에 별도 사본이 있다** — I-17 배치(`enrichment.py::enrich_product` → `artifacts_batch.py::_finish_change`가 `extras`를 만들어 AI Postgres에 upsert)가 그 사본을 만들고, `review_pros`·`situation_tags`는 I-22 홈 추천 reason 조립(`home_recommendation.py::_first_pro`·`_tags`·`build_reasons`)이, `tags`·`attributes`는 임베딩 `search_doc` 조립(`embedding.py::build_search_doc`)이 **그 AI 쪽 사본**을 읽는다(I-1 와이어의 `_extra`를 읽는 게 아니다). `review_cons`·`visual_features`는 AI DB 사본에도 소비처가 없다(grep 0건 — `summary`와 같은 처지) |
| `summary` | ○ | ✗ | `.summary`를 읽는 코드를 `app/`에서 찾지 못했다(grep 0건, seller 쪽 `finding.summary`는 무관한 다른 도메인). 스키마 주석은 "소비는 #101" — #101은 이미 병합됐고(방식2 채택) 그 PR이 실제로 구현한 것은 `attributes` 매칭(PR②)이었다. `summary` 소비 계획이 살아 있는지 별도 이슈로 추적되는 흔적은 못 찾았다. **결론(2026-08-07): 미소비지만 이번 요청에서는 제외(유지)한다** — `_extra`와 달리 AI 쪽에 사본이 없어(I-17이 주지 않는다) 되살리려면 계약 개정이 다시 필요해 되돌리기 비용이 절감분보다 크다 |
| `options`/`optionCount` | ○ | ✗ | 스키마 주석도 "이 PR은 rerank·문구·옵션 되물음에서 소비하지 않는다"(#278)고 명시. 읽는 코드 0건. **다만 #278이 "소비(옵션 되물음 단축 등)는 후속 이슈로 분리"라 적었고, 그 후속이 이번에 #455로 등록됐다 — 소비 계획이 살아 있다.** 이 필드는 애초에 AI가 요구한 것도 아니다 — 정본(Notion) I-1 응답 예시에 먼저 들어와 있었고(본문 prose와 자기모순), #278이 그 모순을 BE에 확인해 "추가 확정" 회신을 받아 AI는 관대 수신만 구현했다. BE는 옵션 수 실측(p99=40·max=161, 20 초과 2.67%가 옵션 바이트의 44.5%)까지 붙여 20개 상한을 설계했다. **[정본 대조]** 정본(Notion I-1)도 2026-08-03 정정으로 `options`를 제외 목록에서 뺀다고 명시한다 — `options`를 요청 대상에서 뺀 이 문서의 판단과 정합한다 |

- **`attributes`의 △가 이 이슈의 핵심**이다 — 소비되는 필드 안에 미소비 하위 키가 들어 있다.
  그래서 "필드 제외"는 최상위 필드 단위가 아니라 **`attributes` 내부 키 단위**여야 한다(§5).
- `summary`·`options`·`optionCount`는 소비 없음이 확인됐지만 **셋 다 최종 요청 대상에서 뺐다**
  (유지, 2026-08-07 결정). `summary`는 `_extra`와 달리 AI 쪽에 사본이 없어, 나중에 소비 계획이
  살아나면 계약을 다시 열어야 하는 비용이 크다. `options`·`optionCount`는 애초에 "당장 소비
  없음"만 확인됐을 뿐 "소비 계획이 아예 없다"를 단정할 근거는 못 찾았는데(없다고 확인한 것이
  아니라 못 찾은 것) — 그 후속 소비 이슈가 **#455로 실제 등록**되면서 계획이 살아 있음이
  확정됐다. 그래서 §5에서도 이 둘을 요청 목록에 넣지 않는다.
- **사전 조사와 달랐던 점**: 없다. 이슈 작성자의 사전 조사(패킷 §2 표)를 항목별로 재확인한 결과
  전부 일치했다.

## §3 미사용 필드의 바이트 기여

표본: `evals/goldenset/fixtures/catalog_snapshot.json`(6,585건). 이 픽스처는 BE 카탈로그와 **같은
원천 덤프**에서 나왔고 `attributes` 모양이 BE가 그대로 내려보내는 JSON과 일치한다(항목 키 자체가
이미 I-1 와이어 필드 이름이다).

**BE가 실제로 그 키들을 내려보낸다는 교차 확인** — 두 근거:

- (a) BE 시드 덤프 `~/inte-final/_sql/mariadb/30_product.sql`의 `product.attributes`에 `_extra`가
  **전 행(6,559/6,559)** 들어 있다 — `grep -c "_extra" ~/inte-final/_sql/mariadb/30_product.sql`
  → `6559`, 헤더 주석 `-- product: 6,559 rows`와 일치(재현 가능).
- (b) BE 응답 DTO가 `attributes`를 가공 없이 통째로(Jackson `JsonNode`) 싣는다 —
  `com.jarvis.product.dto.ProductCandidateResponse`(`record ProductCandidateResponse(..., JsonNode
  attributes, ...)`, BE `origin/main`).

### §3.1 최상위 필드별 바이트 기여 (픽스처 6,585건, 원본 10.700 MiB)

| 필드 | bytes | % |
|---|---:|---:|
| `attributes` | 9,187,313 | 81.9% |
| `name` | 562,283 | 5.0% |
| `summary` | 443,155 | 3.9% |
| `categoryName` | 336,698 | 3.0% |
| `brandName` | 187,106 | 1.7% |
| `productId` | 164,536 | 1.5% |
| `reviewCount` | 134,442 | 1.2% |
| `rating` | 103,913 | 0.9% |
| `price` | 100,322 | 0.9% |

BE 덤프(6,559건, `options`/`optionCount` 없음 — 이 테이블엔 없는 컬럼)로도 교차 확인했다:
`attributes` 76.0%(9,149,015B) · `summary` 13.0%(1,569,523B, 덤프는 픽스처와 달리 대부분 채워져
있어 비중이 크다) — **`attributes`가 지배적이라는 결론은 두 소스에서 같다.**

### §3.2 `attributes` 내부 키별 바이트 기여 (attributes 전체 9,187,313B 대비 %)

| 키 | bytes | % of attributes |
|---|---:|---:|
| `_extra` | 4,885,357 | **53.2%** |
| `_category` | 317,379 | 3.5% |
| `_domain` | 198,550 | 2.2% |
| `_source_pid` | 190,876 | 2.1% |
| (그 외 소재·색상·사이즈 등 카테고리별 속성 축 — 각 2.0% 이하, 길게 흩어짐) | | |

`_extra`(review_pros·review_cons·situation_tags·visual_features) 하나가 attributes 바이트의
과반이고, `_source_pid`·`_domain`·`_category`(AI 사전 매핑 부산물, §2 확인)를 더하면 **attributes의
61.0%(전체의 49.9%)**가 §2에서 소비 미확인인 4개 키에 몰려 있다.

### §3.3 다이어트 적용 시 절감

`attributes` 내부의 `_extra`·`_source_pid`·`_domain`·`_category` 4키를 뺀 결과(`summary`·
`options`·`optionCount`는 전부 유지 — 2026-08-07 결정, §5):

| | 픽스처(6,585건) | BE 덤프(6,559건) |
|---|---:|---:|
| 원본 총 바이트 | 10.700 MiB | 11.476 MiB |
| 다이어트 총 바이트 | 5.367 MiB | 6.167 MiB |
| 절감 | **49.8%** | **46.3%** |
| 1건당 평균(원본) | 1,704 B | 1,835 B |
| 1건당 평균(다이어트) | 855 B | 986 B |

각주: 이 두 데이터소스(픽스처·BE 덤프)에는 **`options` 컬럼 자체가 없다** — 그래서 요청 대상이
`options`·`optionCount`를 포함하는지 여부는 이 표의 수치에 영향을 주지 않는다(빼도 넣어도 같은
값). 위 표는 attributes 4키 단독 효과다.

두 소스가 독립적으로 40%대 후반의 절감을 보인다. `summary`를 유지 대상에서 빼기 전에는(라운드
3 초안) 덤프 쪽 절감률이 더 컸다(덤프는 `summary`가 대부분 채워져 있어 그 필드를 빼는 효과가
더 컸다, §3.1) — `summary`를 유지하기로 하면서 그 차이가 없어지고, 이제는 픽스처가 근소하게
더 크다(아래 편향 참조).

### §3.4 후보 N건일 때 예상 바디 크기 (픽스처 평균 기준, 추정)

| N | 원본(추정) | 다이어트(추정) |
|---:|---:|---:|
| 30 | 49.9 KiB | 25.0 KiB |
| 300 | 499.2 KiB | 250.4 KiB |
| 1,000 | 1.625 MiB | 834.6 KiB |
| 6,585(전량) | 10.700 MiB | 5.367 MiB |

⚠️ N=30/300/1,000 행은 1건당 평균 × N의 **추정치**다 — 실 분포는 상품마다 `attributes` 크기가
달라 균일하지 않다. 실측(§3.1~§3.3)과 구분해 읽을 것.

### §3.5 파싱 시간

`scripts/measure_i1_parse_132.py`(합성 payload, `attributes`+`options`+`summary` 전부 포함하는
무거운 항목 모양)를 로컬에서 재실행한 결과(2026-08-07):

| N | body | elapsed |
|---:|---:|---:|
| 30 | 0.02M | 12.1ms* |
| 1,000 | 0.70M | 11.0ms |
| 3,000 | 2.09M | 47.9ms |
| 7,245 | 5.06M | 92.6ms |
| 20,000 | 13.99M | 398.8ms |

\* N=30 행은 이 스윕의 첫 호출이라 인터프리터·pydantic 워밍업 비용이 섞여 있다(33배 큰
N=1,000보다 느리게 나오는 역전이 그 증거) — N 간 비교에는 쓰지 말 것. N≥1,000 구간의 단조
증가만 추세로 읽는다.

이 스크립트의 합성 항목(698B/건)은 §3.3 실측 평균(1,835B/건, BE 덤프)보다 가볍다 — 절대 바이트는
정확한 대조가 아니다. 다만 **추세**는 분명하다: N=20,000(오늘 카탈로그의 3배 규모)에서도 파싱은
400ms로, 3초 예산에 여유가 크다. **§4의 `size` 상한 필요성은 파싱 시간이 아니라 다른 이유(§4)에서
나온다** — 실측이 그 인과를 정직하게 좁힌다.

`--body-file`로 실 BE 응답을 넣는 대조(원본 vs 다이어트 바디)는 하지 않았다 — 로컬에 BE·DB를
띄우는 실험은 이번 이슈의 비범위(§10)이고, 여기서 시간을 태우지 않기로 했다(패킷 §3 지침).
바이트 실측(§3.1~§3.4)만으로 결론에 필요한 근거는 충분하다.

### §3.6 픽스처 편향 — 우리 주장에 불리한 쪽으로 읽을 것

픽스처의 `summary`·`price`·`rating`·`reviewCount`는 **78.0%(5,138/6,585건)가 null**이다(실측,
네 필드가 정확히 같은 비율인 것으로 보아 같은 배치에서 함께 비어 있다). 이는:

- `price`·`rating`·`reviewCount`의 §3.1 바이트 기여가 **실제보다 작게** 잡혔다 — 실 운영 응답은
  이 세 필드가 대부분 값을 가지므로 절감률 계산에서 분모(원본 총 바이트)가 실제보다 작다. **절감률
  49.8%는 그래서 보수적으로 읽어야 한다**(우리 주장에 유리한 쪽이 아니다).
- BE 덤프 기반 §3.1·§3.3 수치는 이 편향이 없다(`product.price`가 실제 값) — 두 소스를 나란히
  적은 이유다.

## §4 협의 1: `size` 상한 — ❌ 폐지(2026-08-07)

❌ **폐지한다(2026-08-07 결정)** — BE에 요청하지 않으며 재개 계획도 없다. 절단된 상품은 그 턴의
후보에 아예 들어오지 못하고, 결정적 정렬은 재현성만 줄 뿐 관련도를 보장하지 않는다 — 그 손실을
감수할 이유가 없다고 판단했다. 아래 역산·`ORDER BY` 부재·`totalCount` 논증은 지우지 않는다 —
폐지 판단의 근거이자, 나중에 누가 같은 제안을 다시 들고 왔을 때 재론을 막는 기록이다.

**정본(Notion I-1, 2026-07-27 개정)도 같은 결론이다**: *"요청 파라미터 `size`를 없애고 서버
고정 상한도 두지 않는다 … 판매량순으로 자르면 의미 리랭킹과 직교한 기준이 정답 후보를
잘라내기 때문."* 우리 결론이 정본과 독립적으로 같은 곳에 도달했다는 사실이 이 기록의 가치를
높인다.

이슈 본문은 "AI 소비 상한은 `search_max_candidates`(기본 30) 수준이면 충분하다"고 적었다.
**그 이름의 설정은 이 저장소에 없다**(`app/core/config.py` grep 0건 — 한 문서에서 개념적으로
언급될 뿐 실제 설정이 아니다). 그리고 그 수치는 위험하다.

1. **AI는 30건을 "받아서" 쓰는 게 아니라 "전량에서 골라" 30건으로 줄인다.** 흐름: Spring 전량 →
   (fan-out leg 병합·dedup) → 임베딩 유사도 재정렬 → `embedding_rerank_limit`(config 기본 **30**)
   압축 → Sonnet rerank. 이 30은 **재정렬 뒤** 상한이다 — Spring 요청 파라미터가 아니다.
2. **BE 쿼리에 `ORDER BY`가 없다.** BE가 스스로 이렇게 적어 뒀다(`ProductRepository#searchCandidates`
   Javadoc, BE `origin/main`): *"정렬을 걸지 않는 것도 같은 이유(응답 순서 무보장) — 순위는
   FastAPI 리랭킹 소관이라 매칭 전체에 filesort를 거는 비용만 남는다."* 정렬 없는 결과를 앞에서
   자르면 **임의 표본**이다. `MEASURE-I1-RESPONSE-132.md` §3의 `keyword=세트` 실측(매칭 2,657건)을
   기준으로, `size=30`으로 앞을 자르면 사용자가 원한 상품이 후보에 남을 확률은 약 30/2,657 ≈
   **1.1%**다. `size=30`은 12MB 문제를 **품질 붕괴로 바꾸는 것**이다 — #132가 AI 쪽 개수 상한을
   기각한 이유와 같다(`MEASURE-I1-RESPONSE-132.md` §8: *"개수 상한은 두지 않는다 — 전량 반환은
   BE 합의이고, 응답에 ORDER BY가 없어 앞에서 자르면 뒤쪽 관련 상품이 임베딩 재정렬 기회를
   잃는다"*).
3. 그래서 제안은 **(a) 상한값은 크게 + (b) 절단 순서를 결정적으로 + (c) `totalCount` 동반** 3종
   세트다. 하나만 받아들이면 나머지 둘의 위험이 그대로 남는다.

   **(a) 권고값은 3초 예산에서 역산한다** — 바이트 헤드룸이 아니라 **시간 예산**이 근거여야
   한다. 앞선 초안은 "12.3MB의 44%라 안전하다"고 적었는데, **12.3MB는 예산을 초과해 실패한
   값**이다. 실패한 값의 몇 %라는 이유로는 안전을 논증할 수 없다 — 아래처럼 처리율로 역산한다.

   - **처리율**: 이슈 실측 하나뿐이다 — 12.3MB / 7.74s ≈ **1.6 MB/s**(BE 직렬화 + 네트워크
     전송 + AI 파싱을 뭉뚱그린 운영 환경 종단 값). **단일 관측에서 나온 거친 추정**이라는 것을
     명시한다 — 로컬 컨테이너 내부 실측(`MEASURE-I1-RESPONSE-132.md` §3: 13.33MB/1.112s ≈
     12 MB/s)은 loopback이라 이 예산 산정에는 쓰지 않는다(네트워크 구간이 없어 훨씬 빠르다).
   - **남는 창**: 1회 호출 예산은 `spring_timeout_s = 3.0s`(기본, 재시도 0)다. 그중 BE 쿼리가
     이미 최악 1.11초를 쓰므로(`MEASURE-I1-RESPONSE-132.md` §3), **전송·파싱에 남는 창은 약
     3.0 − 1.11 ≈ 1.9초**다.
   - **N=1,000/3,000을 이 창에 넣어 본다**(§3.3 BE 덤프 실측 평균 × N ÷ 1.6MB/s, MB는 처리율과
     같은 10⁶B 단위):

     | | 1건당 | N=1,000 | N=3,000 |
     |---|---:|---:|---:|
     | 다이어트 적용 | 986 B | 0.99MB ≈ 0.6s | 2.96MB ≈ **1.85s — 1.9초 창의 경계에 붙는다** |
     | 다이어트 미적용 | 1,835 B | 1.8MB ≈ 1.1s | **5.5MB ≈ 3.4s — 1.9초 창 초과, 총예산(3.0s)도 초과** |

   **결론이 협의 2(필드 축소)의 수용 여부에 갈린다** — 이게 이 절에서 가장 중요한 대목이다.
   협의 2가 함께 반영되면 N=3,000은 1.85s로 1.9초 창 안에는 들어오지만 그 경계에 바짝 붙는다
   (역산 상한은 1.9×1.6MB/986B ≈ 3,083건). `summary`를 유지하기로 한 결정(2026-08-07,
   `_extra` 등과 달리 AI 쪽에 사본이 없어서다)이 1건당 다이어트 바이트를 이전 라운드 초안보다
   높여(§3.3) 상한 여유를 줄였다는 것을 숨기지 않고 적는다 — 역산 상한(3,083건)이 이제 3,000과
   거의 붙어 있다. **같은 제안이 다시 나온다면 하드 상한은 3,000보다 보수적으로 잡는 편이
   낫다.** 협의 2가 미반영이면 N=3,000(3.4s)은 창을 넘고 총예산도 넘긴다 — 미반영 시 안전한
   상한은 1.9×1.6MB/1,835B ≈ 1,657건이다(이 값은 `summary` 결정과 무관해 그대로다).

   그래서 제안은 **조건부**다:
   - **협의 2가 함께 반영되면**: 권고 기본 **1,000** / 하드 상한 **3,000**.
   - **협의 2가 미반영이면**: 하드 상한을 **1,500 수준**으로 낮춰 주시기 바란다(역산 상한
     1,657건에 여유를 둔 값). 권고 기본은 1,000을 유지해도 무방하다(1.1s로 창 안).

   1,000은 오늘 이미 존재하는 무필터 최악(매칭 7,245건)을 **의도적으로 절단한다** — 그래서
   (b)·(c)가 필수다. 파싱 자체는 §3.5에서 본 대로 이 규모 전부에서 여유가 있어 **제약이
   되지 않는다** — 상한값을 정하는 근거는 파싱 시간이 아니라 **전송을 포함한 종단 시간
   예산**이다(정직하게 밝힌다 — 처음 초안의 "바이트 헤드룸" 논증은 폐기한다).

   **fan-out 유의사항**: 위 표는 **호출 1건** 기준이다. 한 턴은 leg를 최대 5개(`category_fanout_
   max`, config 기본값) 동시에 던지고 완화 probe까지 겹치면 실측 동시 15다(`MEASURE-I1-
   RESPONSE-132.md` §5). 상한은 **호출당**이라 턴 전체가 주고받는 바이트는 그 배수가 될 수
   있다. 오늘은 큰 응답(무필터·단일 호출)과 높은 동시성(리프 카테고리 fan-out·작은 응답)이
   같은 턴에서 겹치지 않지만, `MEASURE-I1-RESPONSE-132.md` §6이 명시하듯 **그 분리는 보장이
   아니라 우연**이다 — 상한값을 (b)·(c) 없이 무한정 키우면 안 되는 이유이기도 하다.

   **(b) 절단 순서** — 무엇이 적절한 정렬 기준인지는 BE가 안다(가격? id? 재고?). AI가 요청하는
   것은 "무엇이든 **결정적**이어야 하고, 그 기준을 알려 달라"이다. 이 요청이 BE에 filesort 비용을
   지운다는 것도 정직하게 밝힌다 — 다만 상한을 두면 그 비용은 **전량 정렬이 아니라 상한 크기의
   top-N 선택**이다(오늘도 이미 `ORDER BY` 없는 그룹핑 쿼리가 최악 1.11초를 쓰고 있다 —
   `MEASURE-I1-RESPONSE-132.md` §3).

   **(c) `totalCount` 필수** — 없으면 AI가 **조용히 틀린다**. 코드로 입증한다:
   `ProductSearchResult.total_count`는 파싱된 항목 수다(`app/services/spring_client.py::
   _parse_search_response`, `total_count=len(products)`) — "매칭 전체 수"가 아니라 **"이번에
   받은 수"**다. 추천 그래프가 이 값으로 다음을 가른다(`app/agents/buyer/recommendation/graph.py`):
   카테고리 확장 후 0건 판정(`decision.category_expanded and search_result.total_count == 0`),
   자동 완화 진입/재판정, 완화칩 `estCount`, 억제-후 재판정(F-1). BE가 `size` 상한으로 절단하는
   순간 이 값은 "받은 수"로 변질되고, AI는 실제로 더 있는 매칭을 "조건에 맞는 상품이 N개뿐"이라고
   오판·안내하게 된다. **이 절이 §7 회귀 테스트 `test_truncated_response_parses_and_total_count_
   follows_received_count`와 짝이다** — 그 테스트가 정확히 이 동작(총량이 아니라 받은 수를
   따라간다는 것)을 코드로 고정한다.

## §5 협의 2: 필드 축소

**최종 요청 대상은 `attributes` 내부 키 4개뿐이다**(2026-08-07 확정) — 아래 1층. `summary`·
`options`·`optionCount`는 전부 유지 판단으로 넘어갔다.

- **1층(가장 큰 효과·가장 안전, 유일한 요청 대상)**: `attributes` 안의 AI 미사용 키 — `_extra`
  (review_pros·review_cons·situation_tags·visual_features)·`_source_pid`·`_domain`·`_category`.
  §3.2 실측으로 attributes 바이트의 61.0%(전체의 49.9%)를 차지한다. §2에서 소비처가 하나도
  없음을 확인했다.
- **유지 판단**: `summary`·`options`·`optionCount` 셋 다 §2에서 같은 미소비 확인을 받았지만
  이번 요청에는 넣지 않는다(2026-08-07 결정).
  - `summary`는 `_extra` 등 attributes 내부 키와 달리 AI 쪽에 사본이 없어(I-17이 주지 않는다),
    지금 빼면 나중에 소비 계획이 살아날 때 계약을 다시 여는 비용이 절감분보다 크다.
  - `options`·`optionCount`는 **소비 계획이 살아 있다** — #278이 "소비(옵션 되물음 단축 등)는
    후속 이슈로 분리"라 적었고 그 후속이 이번에 **#455로 등록**됐다. 이 필드는 애초에 AI가
    요구한 것도 아니다(정본 응답 예시에 먼저 있었고 #278이 BE에 확인해 "추가 확정" 회신을 받은
    것 — §2). 바이트 기여도 작다 — BE 시드 실측 기준 상품당 옵션 이름 약 **120 B**(상품
    3,481개만 옵션 보유, 상품당 평균 6.4개)로, `_extra` 하나가 전체의 44%인 것과 비교가 안
    된다. §8 요청 2에 그 취지를 한 줄로 남긴다. **[정본 대조]** 정본(Notion I-1)도 2026-08-03
    정정으로 `options`를 제외 목록에서 뺀다고 명시한다 — 우리가 `options`를 요청 대상에서
    뺀 판단과 정합한다(§2).
- **2층(대안)**: 위 4키 제외가 어려우면 `fields=` 선택 파라미터나 경량 응답 모드를 검토해 주시기
  바란다.

**바이트 말고도 이유가 하나 더 있다.** BE의 `keyword` 필터가 `attributes` **JSON 전문**을 LIKE로
훑는다 — `ProductRepository#searchCandidates`(BE `origin/main`) 쿼리 조건: `lower(p.attributes)
like lower(concat('%', :keyword, '%'))`. `_extra`의 리뷰 문장·시각 묘사문이 그 LIKE 대상에 들어가므로
**키워드 오탐의 출처**다. 이건 가설이 아니라 BE가 이미 **같은 현상을 `color` 축에서 실측하고
고쳤다** — 같은 쿼리의 Javadoc이 이렇게 적었다(BE `origin/main`, 확정 코멘트 "2026-08-03 LLM팀
실측 합의"): *"구 구현은 attributes JSON 전문을 LIKE로 훑어 정밀도가 화이트 37.9%였다 —
`_extra.visual_features` 설명문이 오탐 출처였다."* `json_extract`로 색상 키만 좁혀 그 오탐을
없앴다(color 축은 이미 해결). **`keyword`는 아직 전문 LIKE다** — 같은 쿼리 안에서 `p.name`·
`p.summary`·`p.attributes` 세 컬럼을 함께 LIKE로 훑는다. 즉 필드 축소는 전송량뿐 아니라 **검색
정밀도와 스캔 비용**에도 듣는다.

이 키들을 빼 주시기 바란다. 다만 다른 소비자(FE 직접 노출, 관리자 도구 등)가 있어 뺄 수 없는
사정이 있다면 알려 주시기 바란다.

## §6 협의 3: rating/reviewCount 비정규화

**조회 시 집계가 맞는지부터 확인**했다. BE의 I-1 쿼리는 `left join Review r ... group by p.id`로
`count(r), avg(r.rating)`을 계산한다(`ProductRepository#searchCandidates`, BE `origin/main`). BE
주석도 이렇게 적는다: *"평점은 같은 쿼리에서 집계(02 D9) — 후보 수가 무제한이라 id IN 배치 집계를
쓸 수 없다."* BE 저장소(`jarvis-backend origin/main`)의 `docs/backend/schema.sql`의 `review` 테이블 주석도 같은 설계를 확인한다:
*"평점 평균·리뷰 수 컬럼 없음 — 조회 시 집계, 반정규화 안 함(D9)."*

제안은 두 갈래이고 **둘은 배타가 아니다**:

- **근본**: 집계 컬럼 비정규화(리뷰 쓰기 시 갱신). 리뷰 11만여 건(#132 실측, 126,313건)에서
  검색마다 조인+집계가 도는 구조다.
- **중간**: `size` 상한이 생기면(§4) BE 주석이 말한 제약("후보 수가 무제한이라")이 풀려 **id IN
  배치 집계 경로로 돌아갈 수 있다** — 그 경로는 BE에 **이미 있다**. `ReviewService#getStats(
  Collection<Long>)`(`reviewRepository.aggregateVisibleByProductIds(productIds)`, id IN 배치)가
  I-3/인기 상품 경로(`ProductService#getPopularCandidates`)에서 이미 쓰인다 — 그 메서드 Javadoc이
  직접 이렇게 적는다(BE `origin/main`): *"I-3 — 인기 상품을 후보 형식으로. 상위 N건 고정이라
  평점은 배치 집계로 충분하다 — I-1과 달리 후보 수가 유한하다."* **§4가 I-1의 후보 수를 유한하게
  만들면, 이 문장의 전제("I-1과 달리")가 I-1에도 성립하게 된다** — 즉 **협의 1이 협의 3의 중간
  해법을 공짜로 연다.** 확인된 사실이라 적는다.

**인덱스 점검도 요청한다.** BE 저장소(`jarvis-backend origin/main`)의 `docs/backend/schema.sql`의 `review` 테이블에는
`idx_review_product`(`product_id` 단일 컬럼) 인덱스만 보인다 — `(product_id, status)` 복합
인덱스는 이 스키마 사본에서는 보이지 않는다. **우리가 단정하지는 않는다** — 이 사본이 라이브 DB의
전체 인덱스(Hibernate 자동 생성분 등)를 다 담고 있다는 보장이 없어, BE가 직접 확인해 주시기
바란다.

**BE 1차 회신(2026-08-07)**: 반정규화는 채택하지 않고 **별도 캐싱 도입**으로 갈음한다. 인덱스는
**OK**(위 우려는 기우였거나 BE가 이미 보완했다는 뜻으로 읽는다). 위 분석(왜 지금이 문제인지)은
지우지 않는다 — 캐싱 결정의 배경으로 그대로 남긴다.

> **[정정, 2026-08-08]** "인덱스는 OK"는 부정확한 기록이었다. 운영 백업 덤프의 DDL로 확인한
> 결과, 이 시점 실제 인덱스는 **`(product_id)` 단독**이었다(`MEASURE-REVIEW-INDEX-395.md` §1) —
> `(product_id, status)` 복합 인덱스는 존재하지 않았다. BE는 이후 PR #133(2026-08-08)에서
> **`status`·`rating`을 더해 `(product_id, status, rating)` 커버링으로 확장**했다. 즉 1차
> 회신의 "OK"는 낙관적 오기였고, 실제로 맞는 상태가 된 것은 08-08이다.

**캐싱은 반정규화와 실패 모드가 다르다는 것을 짚어 둔다.** 반정규화(집계 컬럼 갱신)는 조인 자체를
없애 최악 경로가 사라지지만, 캐싱은 **히트 시에만** 조인을 피한다. 특히 **캐시가 상품 단위이고
미스 시 기존 `LEFT JOIN Review ... GROUP BY` 조인 집계로 폴백하는 구조라면, 이 이슈가 겨눈
최악(무필터 첫 질의 — 매칭 6,559건 전체가 캐시 미스인 콜드 스타트)이 그대로 남는다.** 이건
비난이 아니라 **재측정에서 확인할 지점**이라는 톤으로 적었다 — §8 끝의 "추가 확인 요청"이 그
확인을 위한 질문이었다.

> **이 우려는 BE가 캐싱을 접은 이유 ②와 같은 우려였고, 캐싱이 채택되지 않으면서 대상 자체가
> 사라져 해소됐다.** 아래 최종 회신의 이유 ②가 정확히 이것이다 — *"캐시는 틀려도 에러가
> 안 납니다(무효화가 하나라도 새면 조용히 틀린 평점이 나갑니다)"*. 캐싱 대신 인덱스가
> 채택되어 이 문단이 우려한 콜드 스타트·무효화 실패 모드는 이제 대상이 없다.

**BE 최종 회신(2026-08-08, 노션 페이지 댓글)** — 1차 회신을 스스로 뒤집었다:

> ③ — 1·2는 맞고, 3은 캐싱이 아니라 커버링 인덱스로 처리했습니다.(PR #133 머지 완료)
> `idx_review_product`를 `(product_id, status, rating)` 커버링으로 확장했습니다.
> `before  review: Using where` → `after  review: Using where; Using index`
>
> **왜 캐싱을 안 했는지 — 넷입니다.** ① 캐시가 없앨 비용이 사라졌습니다(인덱스로 집계가
> 인덱스 스캔 하나가 됐습니다) ② 캐시는 틀려도 에러가 안 납니다(무효화가 하나라도 새면
> 조용히 틀린 평점이 나갑니다) ③ 되돌리는 비용이 다릅니다 ④ 병목이 여기가 아닐 가능성이
> 큽니다(7.74초를 0.712초로 만든 건 필드 제거였습니다)
>
> **언제 캐시로 갈지는 정해뒀습니다** — 실측에서 집계가 여전히 병목으로 나오면 그때 붙입니다.

**실측 결과(2026-08-09, 우리가 수행)**: 운영 백업(리뷰 114,077건)에서 A/B/A로 재서 BE의
EXPLAIN 전환(`Using where`→`Using where; Using index`)이 실제로 재현됨을 확인했다 — 무필터
최악(6,290건 집계) DB 레벨 **100.2ms → 26.7ms(3.8배)**, review 페이지 접근 **353,298 →
12,801(27.6배 감소)**. 종단(HTTP)은 **321ms → 233ms(−27%)**로 덜 줄었는데, 이는 응답 바디
크기(11.83MB, #132가 지적한 문제)가 인덱스와 무관하게 그대로 남기 때문이다 — **다음 병목이
있다면 review 집계가 아니라 응답 크기다.** 상세 수치·캡처한 SQL·EXPLAIN 전문은
`docs/specs/MEASURE-REVIEW-INDEX-395.md`가 정본이다. 이 결과는 2026-08-09에 노션 페이지 본문
하단에 회신으로 이미 게시했다(회신 대기 중이 아니다).

## §7 AI 측 수용 준비 상태

아래 6개 테스트(`tests/unit/test_i1_field_diet_395.py`)가 "어느 필드가 빠져도 파서가 견딘다"를
실증한다. 각 테스트가 보장하는 명제를 1:1로 적는다.

| 테스트 | 보장하는 명제 |
|---|---|
| `test_attributes_survives_without_ai_unconsumed_keys` | `attributes`에서 `_extra`·`_source_pid`·`_domain`·`_category`가 빠져도 정상 파싱되고, 소비되는 축(명시 속성 하드매칭)은 살아 있다 |
| `test_summary_missing_still_parses_with_other_fields_intact` | `summary`가 응답에 아예 없어도 파싱되고 나머지 필드(가격·평점·카테고리·브랜드)는 온전하다 |
| `test_options_and_option_count_missing_still_parses` | `options`·`optionCount`가 응답에 없어도 파싱된다 |
| `test_attributes_entirely_absent_still_parses_and_search_unaffected` | `attributes` 자체가 통째로 없어도 파싱되고, 속성 조건 없는 검색 경로가 영향받지 않는다 |
| `test_diet_response_yields_same_post_filter_result_as_full_response` | 미사용 필드가 전부 빠진 "다이어트 응답"도 사후필터(가격·평점·명시 속성 매칭)가 기존 응답과 **같은 결과 집합**을 낸다 |
| `test_truncated_response_parses_and_total_count_follows_received_count` | `size` 절단된 응답도 정상 파싱되며, `total_count`는 매칭 전체가 아니라 **받은 개수**를 따라간다(§4-(c)의 근거를 코드로 고정) |

`summary`는 이번 BE 요청 대상이 아니지만, 미전송 내성 자체는 계속 고정해 둔다(BE가 언제든 뺄 수
있는 선택 필드다).

**공허성 검증(변이 시험)**: 6개 테스트 각각에 대해 관련 필드/로직을 일시적으로 엄격하게 바꿔
테스트가 실제로 깨지는지 확인하고 원복했다(diff에 흔적 없음). 결과:

- `SpringProduct.summary`를 `str`(필수)로 변이 → **6개 전부 실패**(모든 테스트 항목이 summary를
  전송하지 않으므로).
- `SpringProduct.options`를 `list[str]`(필수)로 변이 → **6개 전부 실패**(같은 이유).
- `SpringProduct.attributes`를 `dict[str, object]`(필수)로 변이 → attributes를 아예 안 보내는
  3개 테스트(`test_options_and_option_count_missing_still_parses`·
  `test_attributes_entirely_absent_still_parses_and_search_unaffected`·
  `test_truncated_response_parses_and_total_count_follows_received_count`)만 실패, 나머지 3개는
  attributes를 명시 공급하므로 통과 — 기대한 대로 갈렸다.
- `search_service._matches_attr_conditions`를 항상 `True`를 반환하도록 변이(관대 매칭이 전부
  통과로 퇴화하는 버그를 흉내) → `test_attributes_survives_without_ai_unconsumed_keys`(불일치
  조건이 탈락하는지 보는 음성 단언)와 `test_diet_response_yields_same_post_filter_result_as_full_
  response`(소재 불일치 항목 4가 걸러지는지)가 **둘 다 실패**.
- `spring_client._parse_search_response`의 `total_count=len(products)`를 임의 상수(999)로 변이 →
  `test_truncated_response_parses_and_total_count_follows_received_count`가 **실패**.

## §8 BE 송부용 요청문 2건

그대로 복사해 노션/슬랙에 붙일 수 있는 형태다. 요청 1(`size` 상한)은 폐지되어 송부 목록에서
제외했다 — 근거는 §4.

---

**요청 2 — I-1 응답 필드 축소**

(배경) I-1 응답의 81.9%가 `attributes` 필드이고, 그중 절반 이상(전체의 약 44%)이 `_extra`(리뷰
문장·시각 묘사문 원문)입니다. **그런데 이 데이터는 저희가 잘 압니다 — 저희 쪽 배치(I-17)가
상품마다 만들어 저희 DB에 이미 보관하고 있는 AI 생성물입니다.** `enrichment.py::enrich_product`가
만들고 `artifacts_batch.py::_finish_change`가 저장하며, 그중 `review_pros`·`situation_tags`는
홈 추천 문구 조립(`home_recommendation.py::_first_pro`·`_tags`·`build_reasons`, I-22)이,
`tags`·`attributes`는 검색 임베딩 조립(`embedding.py::build_search_doc`)이 — 저희 DB의 사본을
읽어서 씁니다. I-1 응답의 `_extra`를 읽어서 쓰는 게 아닙니다. 즉 지금은 저희가 이미 만들어 둔
데이터를 귀사가 보관했다가 검색 때마다 돌려주시면 저희가 받아서 버리는 왕복입니다.

(요청) 아래 `attributes` 내부 4개 키를 응답에서 빼 주시기 바랍니다: `attributes._extra`·
`attributes._source_pid`·`attributes._domain`·`attributes._category`. **속성 축(소재·핏·색상 등
`attributes`의 나머지 키)은 계속 필요합니다** — 명시 속성 하드매칭(`search_service._matches_attr_
conditions`)이 그 축을 씁니다. 어려우시면 `fields=` 선택 파라미터 등 대안도 검토 부탁드립니다.

(`summary`·`options`·`optionCount`는 이번 요청에 넣지 않았습니다) 셋 다 현재 AI가 읽는 코드가
없습니다. 다만 `_extra`와 달리 `summary`는 저희 쪽에 사본이 없어서(I-17이 만들어 두지 않습니다)
앞으로 소비할 가능성을 열어 두려고 제외했습니다. `options`·`optionCount`는 옵션 확인이 I-1이
아니라 담기 시점 I-2의 `400 CART_OPTION_REQUIRED` + `options` 목록 경로를 쓰기 때문에 후보
단계에서는 필요 없지만, 저희 쪽에 옵션 되물음 단축용 소비 계획이 있어(#455로 등록) 이번 요청에는
넣지 않았습니다. 바이트 기여도 작아(상품당 옵션 이름 약 120B, `_extra`의 44%와 비교가 안 됩니다)
지금 빼자고 요청할 실익도 크지 않습니다.

(우리가 이미 확인한 것) 위 4개 키가 AI 쪽 어디에서도(오늘 그대로 온다는 가정 하에) 소비되지
않는다는 것을 코드 정적 분석과 회귀 테스트로 확인했습니다(`docs/specs/PROPOSAL-I1-DIET-395.md`
§2·§7). 만약 다른 소비자(FE 직접 노출, 관리자 도구 등)가 있어 못 빼는 키가 있다면 알려 주시면
그 키는 요청에서 제외하겠습니다.

(검색 정밀도) `keyword` 검색이 현재 `attributes` JSON 전문을 LIKE로 훑고 있어 `_extra`의 리뷰
문장·시각 묘사문이 오탐 출처가 됩니다 — 같은 현상을 `color` 축에서는 이미 실측하고 `json_extract`로
고치신 선례가 있습니다(§5). 필드 축소는 전송량뿐 아니라 검색 정밀도에도 듣습니다.

(회신에 필요한 답 항목) ① 위 4개 키 각각 제외 가능 여부, ② 제외가 어려운 키가 있다면 그
이유(다른 소비자 등), ③ `keyword` 검색도 `json_extract`로 좁히는 것이 검토 대상인지.

**BE 회신(2026-08-07)**: **4개 키 전부 수용.**

---

**요청 3 — rating/reviewCount 집계 방식 확인**

(배경) I-1이 매 검색마다 `Review` 테이블과 조인해 평점·리뷰수를 집계하고 있는 것으로 코드에서
확인했습니다(리뷰 12만여 건).

(요청) (1) 평점·리뷰수를 상품에 반정규화(리뷰 작성/수정 시 집계 컬럼 갱신)하는 것을 우선
검토해 주시기 바랍니다 — 검색마다 조인·집계가 도는 구조 자체를 없애는 근본 해법이라고
생각합니다. (2) 그게 지금 당장 어렵다면 참고로 남겨 둡니다 — 향후 후보 수 상한이 논의된다면
(저희 쪽 `size` 상한 요청은 폐지했고 재도입 계획은 없습니다) 이미 BE에 있는 인기
상품 경로의 id-IN 배치 집계(`ReviewService#getStats`)로 I-1도 전환할 여지가 있습니다. (3) 추가로 `review` 테이블에 `(product_id, status)` 복합
인덱스가 있는지도 확인 부탁드립니다.

(우리가 이미 확인한 것) BE 코드 주석에서 "후보 수가 무제한이라 id IN 배치 집계를 쓸 수 없다"는
설계 근거를 확인했고, 그 배치 집계 경로 자체는 인기 상품 조회(I-3)에 이미 구현돼 있다는 것도
확인했습니다. 후보 수 상한이 도입되면 그 제약이 풀릴 것으로 보여 참고로 함께 적어 둡니다.

(회신에 필요한 답 항목) ① 반정규화 검토 여부, ② (참고용) 향후 후보 수 상한이 도입되면 id-IN
배치 집계 전환이 기술적으로 가능한지, ③ `review(product_id, status)` 인덱스 존재 여부.

**BE 최종 회신(2026-08-08, 노션 페이지 댓글 — 1차 회신(2026-08-07 "반정규화 대신 별도 캐싱
도입")을 스스로 뒤집음)**: 캐싱 철회, `idx_review_product`를 `(product_id, status, rating)`
커버링으로 확장(BE PR #133). 실측으로 무필터 최악 3.8배 감소 확인(§6·`MEASURE-REVIEW-INDEX-395.md`).

---

**추가 확인 요청 — 평점 캐싱 4건** ❌ 폐기(2026-08-08 — BE가 캐싱을 채택하지 않아 대상 소멸)
(2026-08-07, 이미 BE에 송부한 문안 그대로)

> **폐기 근거**: BE 최종 회신(2026-08-08, 노션 댓글)이 캐싱 대신 커버링 인덱스(PR #133)를
> 채택해, 이 4건이 묻던 대상(캐시 단위·미스 폴백·무효화 시점·응답 계약)이 사라졌다(§6).
> **본문 4개 항목은 지우지 않는다** — BE가 "실측에서 집계가 여전히 병목이면 그때 캐시를
> 붙인다"는 조건을 남겨 뒀으므로(§6 최종 회신 인용), 캐싱이 나중에 재론되면 그대로 다시 쓸
> 수 있는 질문지이자 이번 폐기 판단의 기록이다.

요청 3 회신(캐싱 도입)을 받고 아래 4건을 추가로 확인 요청했습니다:

① 캐시 단위(상품 단위인지 검색결과 단위인지)와 배치 조회 지원 여부.
② 미스·콜드 스타트 시 기존 조인 집계로 폴백하는지, 폴백이라면 워밍업 계획이 있는지.
③ 무효화 시점(리뷰 작성·수정·삭제·숨김 4경로 각각)과, TTL 방식이면 최대 stale 시간.
④ 응답 계약(필드명·타입·의미) 변화 여부.

③이 AI에 실제로 영향이 있다는 것도 한 줄 남겼습니다 — 평점 하한 필터와 "리뷰가 없어 rating=0"
대 "저평점" 판별(§2 `rating`·`reviewCount` 소비처)이 이 값을 그대로 쓰므로, stale이 길면 방금
리뷰가 달린 상품이 잘못 걸러질 수 있습니다(분 단위 stale이면 무해합니다). 캐싱 설계 자체는 BE
소관이라 저희가 대신 설계하지 않습니다 — 위 확인까지가 저희 몫입니다.

---

## §9 api-spec §4.6 개정 초안

**⚠️ 초안일 뿐 적용하지 않는다.** BE 합의 후 별도 PR에서 `docs/api-spec.md`에 반영한다. 이번
PR은 이 문서만 추가하고 정본은 건드리지 않았다.

`size`·`totalCount` 관련 개정 초안(AI → Spring 요청 표의 `size` 행, 응답 표의 `totalCount` 필드
추가)은 요청 1 폐지(2026-08-07 결정, §4·§8)에 따라 **삭제했다** — 적용할 일이 없는 초안이었다.
아래 `attributes` 축소 diff만 남긴다 — **BE가 이미 수용했다**(§8 요청 2 회신). `rating`/
`reviewCount`는 요청 3 최종 결론이 커버링 인덱스(캐싱 아님)로 바뀌면서 적용할 diff 자체가
없어졌다 — 이유는 아래에서 설명한다.

`[].attributes`의 서술에 아래 문장을 추가(**BE 2026-08-07 수용 — 배포 후 바로 반영 가능**,
타입 변경 없음 — 값 내용만 축소):

```diff
 | `[].attributes` | object \| null | ... 2차 압축 속성 매칭 대상 |
+  **[#395, 2026-08-07 BE 수용]** `_extra`(리뷰 원문·시각 묘사문)·`_source_pid`·`_domain`·
+  `_category`는 AI가 소비하지 않는 내부 키라 BE 응답에서 제외한다(값 형태·타입 계약은 불변).
```

`[].rating`·`[].reviewCount`에 대해서는 **개정 초안을 걷어낸다.** 이전 초안(2026-08-07, BE 1차
회신 "캐싱 도입" 기준)은 *"조회 시 집계 → 캐싱 도입 예정(캐시 단위·TTL·무효화 시점은 확인
중)"*을 적자고 제안했으나, BE가 2026-08-08에 캐싱을 철회하고 **커버링 인덱스(PR #133)**로
최종 결정하면서(§6) 그 전제가 사라졌다 — 이대로 적용됐다면 정본 사본에 사실이 아닌 내용이
들어갈 뻔했다(이 정정의 가장 큰 이유).

**왜 아무것도 적지 않는가(한 줄)**: 커버링 인덱스는 BE 내부 쿼리 최적화라 와이어 계약·필드
의미·타입·최신성 어느 것도 바꾸지 않으므로 api-spec §4.6에 적을 것이 없다 — 기존 서술("조회
시 집계", D9)이 지금도 정확하다.

## §10 비범위·후속

- 골든셋 재실행·실 LLM 호출·DB 기동이 필요한 실험은 하지 않았다(비용·시간, 패킷 §8). 로컬
  픽스처와 정적 근거로 결론을 냈다.
- `--body-file`로 실 BE 응답을 넣는 파싱 시간 원본 vs 다이어트 대조(§3.5에서 언급)는 하지
  않았다 — 로컬에 BE·DB를 띄워야 하는 실험이라 이번 PR 범위 밖이다.
- `docs/api-spec.md` 정식 개정, `app/schemas/spring.py`·`spring_client.py`의 실제 필드 축소
  구현, `app/core/config.py`에 `size` 상한 관련 튜너블 추가는 **BE 합의 후** 별도 이슈·PR로
  진행한다.
- `summary`가 향후 소비될 계획(#101 2차 압축 등)이 실제로 살아 있는지는 이 이슈에서 확정하지
  못했다. `_extra`와 달리 AI 쪽에 사본이 없어 되살리는 비용이 커 이번 요청 대상에서는 뺐다
  (2026-08-07 결정, §2·§5) — BE에 직접 질문으로 올리지는 않았다(요청 2 회신 항목에서 제외).
- **2026-08-07 사용자 결정**: 요청 2에서 `summary`는 제외 요청 대상에서 뺀다(= BE에 유지 요청)
  — `_extra` 계열과 달리 AI 쪽에 사본이 없어(I-17이 안 준다) 되살리려면 계약 개정이 다시
  필요해 되돌리기 비용이 절감분보다 크다(§2·§5).
- **2026-08-07 사용자 결정(라운드 6에서 확정)**: 요청 1(`size` 상한)은 보류가 아니라 **폐지**다
  — BE에 요청하지 않으며 재개 계획도 없다. 근거 분석(§4)은 폐지 판단의 기록으로 그대로 남긴다.
- `options`·`optionCount`도 최종적으로 요청 대상에서 뺐다(2026-08-07 확정, §2·§5) — 소비 계획이
  아예 없는 게 아니라 **살아 있음이 #455 등록으로 확정**됐고, 바이트 기여도 작아 지금 빼자고
  요청할 실익이 크지 않다.
- **정본 대조로 드러난 사실**(부모 세션이 Notion I-1 정본을 직접 읽어 확인. 이번 PR 범위에서는
  이 제안서에만 반영하고 `docs/api-spec.md`는 건드리지 않는다 — 사본 동기화는 사람 승인 게이트라
  별도 작업이다):
  - **`rating`/`reviewCount` — 사본 동기화 대상**: 정본은 리뷰 0건이면 `rating: 0.0`·
    `reviewCount: 0`(I-17과 동일 규약)으로 확정한다. 이 문서 §2의 사본 서술은 "null/미전송이면
    rating이 지배"라 null과 0을 둘 다 상정한다 — `docs/api-spec.md` §4.6의 해당 서술을 정본에
    맞춰 정정하는 작업이 후속으로 필요하다(§2 각주 참조, 지금 고치지 않는다).
  - **`brandName` 부분 매칭 규칙 — 미해결**: 정본에 "⚠️ LLM 팀 확인 필요"로 걸려 있다. 답을
    여기서 정하지 않는다 — 우리가 답해야 할 항목으로 등재만 하고, 사람 결정을 기다린다.
- **등록된 후속 이슈 두 건**(이번 라운드에 확정):
  - **#454** — 옵션 단위 재고를 몰라 "그 색만 품절"인 상품을 추천할 수 있다(BE 협의 필요,
    `blocked:spring`). 이 문서의 범위(I-1 응답 다이어트)와는 별개 문제라 여기서는 다루지 않는다.
  - **#455** — I-1 `options`·`optionCount` 소비로 옵션 되물음 단축(#278 후속, 이번에 등재). 위
    "확정 사실 1"이 이 이슈에 기대어 `options`·`optionCount`를 요청 대상에서 뺀 근거다.
- **BE 반영 후 재측정 절차**(이슈 #395 완료 조건 "무필터 요청의 응답 시간이 3초 예산 안에
  들어온다(재측정)"을 닫는 데 쓴다) — **트리거: BE가 §8 요청 2(필드 축소)·요청 3(커버링
  인덱스, PR #133)를 배포한 뒤** 아래 절차를 수행한다(그 전에는 재측정해도 의미가 없다 —
  다이어트 전 응답을 재는 꼴이다). 새 스크립트를 만들지 않고 기존 것으로 한다:
  (a) `X-Internal-Token`으로 `/internal/products/search`를 무필터 최악 질의로 직접 호출해
  바디 크기·소요를 잰다(`MEASURE-I1-RESPONSE-132.md` §3이 쓴 방식 그대로 — BE가 로컬/스테이징에
  기동돼 있어야 한다). (b) 그 응답 바디를 파일로 저장해 `scripts/measure_i1_field_bytes_395.py
  --dump`가 아니라 **저장한 실 응답 자체**를 육안·`jq`로 확인해 §5가 요청한 필드(`_extra` 등)가
  실제로 빠졌는지 대조하고, 동시에 `scripts/measure_i1_parse_132.py --body-file <저장한 응답>`
  으로 파싱 소요를 잰다(§3.5가 이미 이 스크립트를 쓴다 — 다이어트 반영 전/후 바디를 각각 넣어
  대조). (c) 같은 무필터 질의를 2회 연속 호출해 반환된 `productId` 순서(상한 절단 시 앞 N건)가
  두 응답에서 동일한지 확인한다 — 다르면 §4-(b)가 요청한 "결정적 정렬"이 지켜지지 않은 것이다.
  ~~(d) 콜드 스타트 확인(§6 캐싱 우려의 실측) — 배포 직후(캐시가 비어 있을 시점)에 (a)를
  실행해, 캐시 미스 폴백이 이 이슈가 겨눈 최악(무필터 첫 질의)을 그대로 재현하는지 확인한다.~~
  **(d)는 대상 소멸이다** — BE가 캐싱이 아니라 인덱스를 채택해(§6) 캐시 미스·콜드 스타트라는
  개념 자체가 없어졌다.

  **이 절차 중 인덱스(요청 3) 쪽은 이미 수행됐다**(2026-08-09) — `docs/specs/
  MEASURE-REVIEW-INDEX-395.md`가 결과 정본이다. (a)에 해당하는 무필터 최악 실측은 DB 레벨
  100.2ms→26.7ms·종단 321ms→233ms, EXPLAIN 전환도 재현됐다(§6). (b)·(c)는 필드 축소(요청 2)
  검증 항목이라 이번 실측 범위 밖이다 — 별도로 수행된 기록을 찾지 못했다.

  **완료 조건 판단(한 줄)**: 필드 축소 효과(7.74s→0.712s)는 BE가 직접 보고했고(§6 최종 회신
  인용), 커버링 인덱스 효과는 우리가 직접 실측했다(§6·`MEASURE-REVIEW-INDEX-395.md`) — 두
  축 다 "3초 예산 안"이라는 결론에는 이미 도달했다고 판단한다. **[2026-08-10 갱신] 아래 문단이
  남겨둔 (b)·(c) 미실시는 §11 2026-08-10 운영 실측으로 해소됐다** — (b) 필드가 실제로 빠졌는지
  프로그램 대조(4키 6,128건 전건 부재)·(c) 정렬 결정성 확인(무필터 2회 연속 호출, `productId`
  순서·내용 완전 일치) 둘 다 §11에서 수행했다. 다만 (c)는 **BE 쿼리에 `ORDER BY`가 없어
  계약상 보장이 아니라 그 시점 관측**이라는 단서는 그대로 유지한다. 절차를 문자 그대로
  완주했다는 판단은 이제 근거가 있다(추측이 아니다).

### 이슈 #395 완료 조건 대비 현재 상태 (2026-08-10 갱신, 최초 작성 2026-08-07)

| 항목 | 상태 | 비고 |
|---|---|---|
| 필드 축소(`attributes` 4키) | **배포 확인 + 운영 실측 완료**(2026-08-10 운영 실측 — 무필터·keyword 두 질의 6,506건 전건 4키 부재, 항목당 1,835B→1,105.7B) | §8 요청 2, §9 diff 반영, §11 |
| rating/reviewCount | **BE 캐싱 철회 → 커버링 인덱스로 최종 확정**(PR #133), 실측 완료 | §6·`MEASURE-REVIEW-INDEX-395.md`. §9 diff는 없음(적을 것이 없음) |
| `review` 인덱스 | **정정: 당시 `(product_id)` 단독이었다** → PR #133에서 `(product_id, status, rating)` 커버링으로 확장, 실측 확인 완료 | §6(1차 회신 "OK"는 부정확한 기록이었음) |
| `size` 상한 | **❌ 폐지 + 정본(`docs/api-spec.md` §4.6) 반영 완료** — 재개 계획 없음 | §4, 근거만 기록 보존(§8 요청 1 문안은 삭제) |
| 완료 조건 "무필터 3초 예산 재측정" | **실시 완료**(2026-08-10 운영 실측, §11) — 중앙값 2.09s로 예산 안이나, **콜드 첫 호출은 3.203s로 예산을 스칠 수 있다**("항상 예산 안"은 아니다). 절차 (b)·(c)도 §11에서 함께 수행 | 위 "완료 조건 판단" 참고 |
| 후속 이슈 | #454(옵션 재고, blocked:spring)·#455(옵션 소비, 등록) | 이 문서 범위 밖, 추적만 |

## §11 배포 후 라이브 검증 (2026-08-10)

> ⚠️ 이 절은 **측정 시점의 기록**이다(문서 맨 앞 경고와 같은 톤) — 아래 수치는 2026-08-10
> 시점 운영 카탈로그·트래픽 상태를 반영한 1회성 실측이며, 카탈로그가 바뀌면 절대값은
> 달라진다. 인용할 때는 배수·판정(예산 안/스침)을 우선하고 절대 ms·B 는 "이 시점 값"으로
> 읽을 것.

**방법**: 운영 AI 컨테이너(`jarvis-ai`, EC2) 안에서 운영 Spring의 `GET /internal/products/search`
를 **프로덕션과 같은 httpx 경로**(`app.services.spring_client._client`)로 호출해 쟀다 — `urllib`
로 직접 호출하면 403이 나 프로덕션 경로가 아니면 재현이 안 된다. 재현 스크립트는
`scripts/measure_i1_live_395.py`(`--keyword`·`--repeat`·`--timeout` 인자).

### (a) 필드 구성 — 4키 전건 부재 확인

| 질의 | 항목 수 | 4키(`_extra`·`_source_pid`·`_domain`·`_category`) 보유 항목 |
|---|---:|---:|
| 무필터(전 파라미터 생략) | 6,128 | **0 / 6,128 (전건 부재)** |
| `keyword=셔츠` | 378 | **0 / 378 (전건 부재)** |

최상위 필드는 11개 전건 존재: `productId`·`name`·`summary`·`attributes`·`categoryName`·
`brandName`·`price`·`rating`·`reviewCount`·`options`·`optionCount`. `attributes`는 6,128건
전건 존재하며 상위 키는 `브랜드`(6,128)·`제조국`(5,625)·`색상`(4,970)·`사이즈`(4,268)·
`소재`(4,008) 등 **카테고리 속성 축만** 남았다.

### (b) 항목당 바이트 · 필드별 분해 (무필터 응답 1건 기준)

- HTTP 바디 **6,424,455 B (6.13 MiB)**, 항목 6,128건, **항목당 평균 1,105.7 B**(min 610 /
  max 14,261). §3.3 다이어트 전 기준선 **1,835 B/건**(BE 시드 덤프) 대비 **−39.7%**.
  ⚠️ 항목당 바이트는 `json.dumps(item, ensure_ascii=False)` 기본 구분자(`, `/`: `) 기준이라
  실제 와이어보다 약간 크다(항목 합 6,775,558 B vs HTTP 바디 6,424,455 B) — §3과 같은 계산
  규약이므로 **비교는 유효**하다.
- §3.3 예측치는 986 B/건이었는데 실측이 1,105.7 B/건인 이유는 **§3.3이 쓴 두 데이터소스
  (픽스처·BE 시드 덤프)에 `options`/`optionCount` 컬럼 자체가 없었기 때문**이다(§3.3 각주가
  이미 그렇게 적었다). 실측에서 `options`+`optionCount`는 **47.8 B/건**이고, 986 + 47.8 ≈
  1,034 B로 나머지 차이는 카탈로그가 그사이 바뀐 몫이다.

최상위 필드별 바이트(값만, 항목 합 6,775,558 B 대비):

| 필드 | bytes | % |
|---|---:|---:|
| `attributes` | 3,275,128 | 48.3% |
| `summary` | 1,384,368 | 20.4% |
| `name` | 459,945 | 6.8% |
| `options` | 286,462 | 4.2% |
| `categoryName` | 203,565 | 3.0% |
| `brandName` | 79,016 | 1.2% |
| `productId` | 61,191 | 0.9% |
| `price` | 31,476 | 0.5% |
| `rating` | 18,384 | 0.3% |
| `reviewCount` | 7,374 | 0.1% |
| `optionCount` | 6,553 | 0.1% |

`attributes` 내부 키 상위(attributes 총 3,275,128 B 대비): `색상` 112,966(3.4%) · `전성분`
97,630(3.0%) · `사이즈` 96,138(2.9%) · `옵션구성` 93,001(2.8%) · `세탁방법` 86,229(2.6%) ·
`브랜드` 77,510(2.4%) · `소재` 71,966(2.2%) — **4키 잔존 바이트는 전부 0 B.**

→ 다이어트 전 `attributes` 비중 **81.9%**(§3.1, 픽스처)에서 **48.3%**로 내려왔고, 이제 2위
기여자는 `summary`(20.4%, 225.9 B/건)다. `summary`는 2026-08-07에 "AI 쪽 사본이 없어 되살리는
비용이 크다"로 유지 결정이 끝났다(§10) — 재론하지 않는다.

### (c) 소요시간 — 무필터 최악, n=9

운영 AI 컨테이너에서 운영 Spring까지의 왕복(본문 수신 완료까지) 실측, 9회:

```
1.918 1.939 1.965 1.987 2.094 2.133 2.600 2.978 3.203  (초, 정렬)
```

- 중앙값 **2.094 s**, 최소 1.918 s, 최대 **3.203 s**(그 세션의 **첫 호출**), 9회 중 3.0 s
  초과 **1회**.
- `keyword=셔츠`(378건, 411,668 B)는 n=7에서 중앙값 **0.239 s**, 최대 0.291 s.
- 운영 런타임 실값: `spring_search_timeout_s = 3.0`, `spring_timeout_s = 3.0`,
  `spring_max_retries = 0`.
- 협의 착수 시점(2026-08-06) BE 실측은 **7.74 s · 12.3 MB**였다 → 지금 **2.09 s(중앙값) ·
  6.13 MiB**.

**판정**: 통상 구간은 3 s 예산 안이지만 **콜드 첫 호출은 예산을 스칠 수 있다(3.203 s)** —
"항상 예산 안"은 아니다.

### (d) 정렬 결정성

무필터를 연속 2회 호출해 `productId` 배열을 비교한 결과 **순서·내용 완전 일치**(6,128건) —
§10 절차 (c)가 요구한 확인이다. 단, **BE 쿼리에 `ORDER BY`가 없다**는 사실은 그대로이므로
"결정적 정렬이 계약으로 보장된다"가 아니라 **"2026-08-10 실측에서는 두 호출이 동일 순서였다
(계약상 보장은 아니다)"**로 읽는다.

### 요약 — §8 요청 2·요청 3 배포 후 종결

필드 축소(요청 2)·커버링 인덱스(요청 3) 둘 다 배포됐고, 위 (a)~(d) 실측으로 **이슈 #395의
남은 확인 항목(§10 절차 (b)·(c))이 해소됐다.** `size` 상한(요청 1)은 폐지가 확정이라 재측정
대상이 아니다. `docs/api-spec.md` §4.6이 이 결과를 반영한 정본 사본이다.
