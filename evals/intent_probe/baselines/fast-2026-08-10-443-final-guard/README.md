# fast-2026-08-10-443-final-guard

**#465 후처리(총칭 head 억제)의 안전성을 최신 프롬프트에서 재확인한 런.** 11셀
(`named_category` 6 + `condition_only` 5) × N=24 · `fast` · `prompt.source=repo:_SYSTEM` ·
프롬프트 sha12 **`a3f8f26cbb6e`**.

| 축 | 값 |
|---|---|
| `namedCategoryHasLeg` | 104/144 (72.2%) |
| `conditionOnlyNoCategoryQuery` | 115/120 (95.8%) |

## 왜 전량 재측정이 아니라 이 런인가

세션 중 `origin/dev` 백머지로 `_SYSTEM` 이 **세 번** 바뀌었다
(`865ed6fd771e` → `5d5a71b0ab09` → `a3f8f26cbb6e`). 매번 전량 재측정하면 dev 속도를 따라갈 수
없어, 채택 근거를 **프롬프트 의존/비의존으로 갈랐다**:

- **비의존** — 안전성(정당한 상품 leg 을 지우지 않는다)은 술어의 구조적 성질이다. 상품명은 총칭
  head(`거·것·상품·제품·아이템·아무거나`)를 갖지 않는다. 비용도 0(결정론, LLM 호출 없음).
- **의존** — 이득의 크기(발동률)뿐이고, 그건 `fast-2026-08-10-465-REPORT.md` 에 "표적이 런당
  2~3건" 으로 이미 적혀 있다.

그래서 이 런은 **의존 부분만** 최신 프롬프트에서 확인한다. 결과: 보호 대상 `named_category`
단일 leg 104표본에서 **오발동 0건**, 표적 `condition_only` 누출 leg 5건 중 4건 억제.
세 프롬프트 판을 합치면 보호 대상 누적 **181표본 · 오발동 0건**이다.

## 이 표로 읽으면 안 되는 것

`namedCategoryHasLeg` 104/144(72.2%)는 **#443 결함이 최신 프롬프트에도 남아 있다**는 뜻이지
후처리의 성능이 아니다(후처리는 #465 방향 전용이다). #443 처방의 실측은
`fast-2026-08-10-443-{base-2,inject-1,inject-2}` 와 `PREREGISTRATION-443-inject.md` 에 있다.
