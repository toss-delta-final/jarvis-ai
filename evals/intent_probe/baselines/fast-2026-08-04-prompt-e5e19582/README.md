# #240 기준선 프롬프트 대조 런 — 2026-08-04

`--prompt-rev 3f1dec7` 로 **#240 이 쟀던 그 프롬프트**(sha12 `e5e195822495`)를 넣고 같은 앵커(B)·
같은 N(8)으로 돌린 런. 목적은 프롬프트 평가가 아니라 **하네스 검산** — 답이 알려진 과거 측정을
다시 재서 이 도구가 같은 것을 재는지 확인한다.

53셀 · 424표본 전부 채움 · **실패 0** · USD 0.076.

## #240 코멘트 표와의 대조

| 축 | 이번 | #240 | 차이 |
|---|---|---|---|
| `mainIntent` | 237 | 237 | **0** |
| `cartControl` | 144 | 144 | **0** |
| `demonstrative` | 93 | 93 | **0** |
| `optionAnswer` | 27 | 27 | **0** |
| `orderStatus` | 48 | 48 | **0** |
| `general` | 37 | 32 | +5 |
| `switchLegacy2` | 0 | 8 | −8 |
| `cartAddProductIdLegacy2` | 5 | 15 | −10 |

**5개 축이 정확히 일치한다.** 같은 프롬프트 해시로 재면 같은 숫자가 나온다는 뜻이고, 하네스가
#240 과 같은 것을 재고 있다는 강한 신호다.

## 어긋난 3축은 전부 '재구성한 부분'이다

원 픽스처가 유실돼 상품명·id 는 이슈 본문에서 **추정 복원**했는데, 어긋난 축이 정확히 그
추정에 의존하는 축이다 — 전환(switch)은 "무엇으로 갈아탈지"를 상품명 어휘로 가리키므로
목록 상품명이 다르면 그대로 갈린다.

옛 프롬프트에서 전환 발화의 intent 분포(pendingCart):

```
이어폰으로 할래   cart_add 1 / recommend 7      → productId 은 7회 null
다른 거 담아줘    cart_add 4 / cart_view 2 / recommend 2
```

즉 옛 프롬프트는 이 앵커에서 전환 발화를 **cart_add 로 보지 않는다.** 그래서 productId 를
채점하는 두 축이 함께 무너졌다(0/16, 5/16). 같은 앵커를 **현재 프롬프트**로 재면
`이어폰으로 할래` 는 cart_add 8/8 이고 productId 도 이어폰(104)을 7회 고른다 —
`switchLegacy2` 10/16. 그 사이에 #253(되물음 중 전환을 소비처 코드로)과 프롬프트 개정이 있었다.

**결론**: 이 대조는 #240 표를 "재현했다"가 아니라, **정답지가 조금 다르면 특정 축이 통째로
갈린다**는 이 이슈의 논지를 다시 확인해 준다. 그래서 앵커를 커밋한다.

## 부수 확인 — 페이서 기본값 수정이 듣는다

같은 규모(424콜)를 직전 런은 50rpm · 콜당 3.1k 추정으로 돌려 429 를 78회 먹었고, 이 런은
45rpm · 콜당 3.9k(= max_tokens 예약분 포함)로 **실패 0** 이었다.
