# 구매자 골든셋 변경 이력

아직 baseline 실행·공개 전인 v1 초안이므로 아래 정정에도 `datasetVersion`은 `1.0.0`을 유지한다.

| 날짜 | datasetVersion | 변경(add/remove/relabel) | caseId | 사유 |
|---|---|---|---|---|
| 2026-08-02 | 1.0.0 | add | dev: buy-srch-0001~0006, buy-gust-0001~0002, buy-cold-0001~0002, buy-pers-0001~0003, buy-over-0001, buy-repu-0001~0003, buy-cmap-0001~0006, buy-mult-0001~0002, buy-fail-0001~0003; holdout: buy-srch-1001~1006, buy-cmap-1001~1002, buy-fail-1001~1002, buy-repu-1001, buy-pers-1001 | 구매자 추천 골든셋 v1 최초 40건(dev 28 / holdout 12) 구축 |
| 2026-08-03 | 1.0.0 | add | buy-over-0002, buy-over-0003 | 카테고리·브랜드 축의 personalization_overreach 회귀 케이스 보강 |
| 2026-08-03 | 1.0.0 | add | buy-srch-0007 | 야구 categoryName과 실제 장비를 snapshot에서 검증하는 검색 케이스 보강 |
| 2026-08-03 | 1.0.0 | relabel | buy-srch-0005 | 카테고리만 오분류된 실제 축구공 2건을 금지 결과에서 정답(3/1등급)으로 재판정 |
| 2026-08-03 | 1.0.0 | relabel | buy-cmap-0005, buy-cmap-0006 | forbiddenCategories를 DB 계층명이 아닌 I-1 categoryName 표기로 정정 |
| 2026-08-03 | 1.0.0 | relabel | buy-over-0001 | 사용자가 7만원 이상 제약을 직접 말하도록 질의를 재작성해 priceMin 근거를 정정 |
| 2026-08-03 | 1.0.0 | relabel | buy-srch-1001~1006, buy-cmap-1001~1002, buy-fail-1001~1002, buy-repu-1001, buy-pers-1001 | holdout 판정 notes를 봉인 라벨 파일로 이동 |
| 2026-08-03 | 1.0.0 | relabel | guest identity 전 30건 | identity.kind와 guest slice를 양방향 정합 |
| 2026-08-03 | 1.0.0 | relabel | 전체 43건 fixture | request를 raw 발화가 아닌 expectedFilters로 라이브 재기록 |
| 2026-08-03 | 1.0.0 | relabel | buy-srch-1002, buy-cmap-1001, buy-srch-1003 | 오답 후보가 함께 검색되도록 expectedFilters를 상위 keyword로 넓히고 정답을 재판정 |
| 2026-08-03 | 1.0.0 | relabel | buy-srch-1004 | 단일 후보 질의를 학생 운동화 질의로 교체하고 라이브 후보에서 정답을 재판정 |
| 2026-08-03 | 1.0.0 | audit | nonDiscriminativeRanking | 후보가 전부 정답인 케이스를 #143 순위 지표 분모 제외 목록으로 감사 산출물에 추가 |
| 2026-08-05 | 2.0.0 | migrate | 전체 43건(dev 31 / holdout 12) | #333 Part 1 — `migrate_v1_to_v2.py`로 v1(1.0.0)을 v2(2.0.0)로 기계 이관. `testType=MFT` 명시, 니즈 슬라이스(single_need/multi_constraint/budget/repurchase)를 케이스당 정확히 1개 배정, `identity.kind==member` 케이스에 `member` 슬라이스 거울 추가, `fixtures/search_responses.json`에 v2 candidates provenance(`source`/`rule`/`from`) 채움. 니즈 슬라이스 우선순위는 패킷 문면(가격 하드제약 최우선)과 달리 **v1에 이미 명시적으로 curation된 `multi_constraint`/`repurchase` 슬라이스 태그를 가격 신호보다 우선**했다 — 문면 그대로면 `buy-mult-0001/0002`·`buy-fail-0001/0003`가 전부 가격 제약도 있어 `budget`으로 흡수되고 dev split에 `multi_constraint` 케이스가 0건 남아 `audit.run_audit()`의 `missingDevSlice` 위반이 새로 생겼다(상세는 구현 보고 §설계 결정, orchestrator 확인 필요). |
| 2026-08-05 | 2.0.0 | relabel | 순위 평가 대상 중 후보 20건 미만 20건(dev 14 / holdout 6) | `narrow-domain:` notes 접두 문구로 `goldenset_min_ranking_candidates` 하한 예외 처리. Part 1은 구조·규약만 다루므로 실제 후보 패딩(하드 네거티브 주입)은 Part 2에서 한다 — 이 표시는 임시이며 Part 2가 후보를 30까지 채우면 대부분 해제되어야 한다. |
| 2026-08-05 | 2.1.0 | relabel | 전체 43건(dev 31 / holdout 12) | #333 Part 2 §3-1 — `evals/goldenset/campaign_v2.py`(신설, 커밋 대상 하네스)로 v1 43건 전부를 현 카탈로그(pg-catalog 6,559건, v1 시드 7,220건에서 축소) 기준 라이브 재기록. 골든(limit 30) + 완화(keyword-only/category-only, `--relaxed-limit` 120) 검색 후 `inject.build_case_candidates`로 하드 네거티브(semantic_near 실측 최대 채널·price_violation·attr_violation·other_brand·random_catalog)를 채워 전 케이스 후보 depth=30 균일화(`narrow-domain:` 잔존 마커는 후보 자체가 2~3건뿐인 진짜 좁은 도메인 3건만 남김). 사라진 v1 라벨 상품은 재검색 결과로 자연 재라벨됐다(개별 productId 치환 없음, 상품명 매칭 기준 재판정). |
| 2026-08-05 | 2.1.0 | relabel | buy-cmap-0005, buy-cmap-0006, buy-over-0002 | Spring I-1 `categoryName`이 일부 카테고리에서 `대분류 > 소분류` 계층 표기로 바뀌어(v1 시점엔 평탄 표기) `forbiddenCategories`(평탄 표기 전제, `_categories_use_i1_notation` validator가 `" > "` 자체를 거부)가 라이브 스냅샷과 어긋남 — `forbiddenCategories`를 비우고 `mustExcludeProductIds`만으로 금지를 유지, notes에 발견 경위를 남김. 스키마 validator/GUIDE의 평탄 표기 전제 자체를 바꾸는 계약 판단은 이번 Part 범위 밖(오케스트레이터 확인 필요, 최종 보고에 리스크로 명기). |
| 2026-08-05 | 2.1.0 | add | dev: buy-srch-0008~0009, buy-mult-0003~0010, buy-budg-0001~0010, buy-repu-0004~0008, buy-pers-0004~0007 (29건, labeler-02) | #333 Part 2 §3-3 — 슬라이스 쿼터 충족용 순위 판별 dev MFT 신규(guest 10 / member 19). 신규 member 페르소나 13건(구매이력 보유) + 16건(빈 이력) `purchase_history.json`에 추가. 3건(`buy-budg-0009/0010`, `buy-pers-0005`)은 페르소나 이력과 명시 발화가 모순되는 `personalization_overreach` 회귀 케이스. |
| 2026-08-05 | 2.1.0 | add | dev INV/DIR 36건: buy-invc-0001~0012(color_synonym 6쌍, #258 color_synonyms 동의어), buy-invw-0001~0012(word_order 6쌍), buy-dirc-0001~0006(constraint_subset 3쌍, 완화/강화는 expectedFilters 키 추가로 판별), buy-dirm-0001~0006(member_recall_ge_guest 3쌍, 라벨 필요) | #333 Part 2 §3-3 — CheckList 규약(라벨 없이 규모를 늘리는 축) 18그룹. `evals/metrics/behavior.py`가 검사. 결정론 scripted 실행에서 color_synonym/word_order/member_recall(동일 fixture·라벨 공유) INV/DIR은 구조적으로 통과한다는 한계는 GUIDE에 이미 명문화돼 있음(#333 리뷰 F-3) — 실모델 검증 근거로 인용하지 않는다. |
| 2026-08-05 | 2.1.0 | add | holdout: buy-cmap-1003~1006, buy-fail-1003~1005, buy-repu-1002~1006 (12건, labeler-02, 신규 라벨은 `buyer_holdout_labels.jsonl`에만) | #333 Part 2 §3-3 — holdout 목표 24건(guest 12/member 12) 달성. `buy-repu-1002~1006`은 personalization 신호도 겸한다(재구매 시나리오). confirmatory 슬라이스별 ≥6 목표 중 `search`(19)·`personalization`(7)·`repurchase`(6, 정확히)·`category_mapping_failure`(6, 정확히)는 달성, `failure`만 5(목표 미달 1) — holdout 총량이 24건으로 고정돼 있고 `category_mapping_failure`(실 결과 필요)와 `failure`(0건 필요)가 상호배타적이라 남은 7~8슬롯으로 두 태그 8건을 동시에 채울 수 없는 수학적 한계(최종 보고에 명기). |
| 2026-08-05 | 2.1.0 | relabel | 전 케이스(120건) | `datasetVersion` 2.0.0→2.1.0 상향(schema.py `DATASET_VERSION`·`Literal` 동시 갱신). manifest.json 재계산(counts/labelers에 labeler-02 추가/provenance/파일 해시/datasetHash), `run_audit(write=True)` 재실행(0 violation, 경고 16건은 전부 동일 split 내 queryNearDuplicate·holdoutRatioDrift로 무해). |
| 2026-08-05 | 2.1.0 | fix | dev INV/DIR constraint_subset 6건(buy-dirc-0001~0006) | #333 Part 2 라운드2 F-R6 — dir-subset-03(스니커즈) 실측 실패(behaviorChecks 18/18 미달) 근본 수정. fixture `productIds` 집합 수준의 부분집합만으로는 평가 시점 push 절단(`LIST_MAX_PRODUCTS=9`, productId ascending 상위 K)을 통과하지 못함을 확인(`inject.merge_relaxed_with_stricter_floor` docstring 참조) — 강화(stricter)를 먼저 라이브 기록하고, 완화(relaxed)가 강화 쪽 candidates 전원 + (강화 쪽 최대 productId보다 큰 것만) 병합하도록 `campaign_v2.py`에 `constraintPairOf` 스펙 필드를 신설해 재기록. 완화/강화 top-K 노출이 항상 동일 집합이 되도록 구성적으로 보장. `buy-dirc-0003/0004`(원피스 쌍)는 판별력이 약해(색상 필터는 catalog에 구조화 필드가 없어 로컬 재검증 불가) `color` 대신 `priceMax` 제약으로 교체. `schema.validate_cases()`에 constraint_subset 그룹 부분집합 검증 신설(로드 시점에 재발 감지). |
| 2026-08-05 | 2.1.0 | add | dev: buy-srch-0010~0014, buy-pers-0008~0009 (7건, labeler-02) | #333 Part 2 라운드2 F-R3 — ranking-eligible(러너 실측) `guest` 26건·`member` 29건이 `MIN_CONFIRMATORY_SLICE_N`(30) 문턱에 근소 미달해 `confirmatory` 라벨을 못 받던 것을 해소(guest 5건·member 2건 신규로 각 31건 확보, 여유 1건씩). 신규 member 페르소나 2건(구매이력 없음) `purchase_history.json`에 추가. 니즈 슬라이스는 이미 쿼터를 초과 달성한 `single_need`에 배정(재량, 별도 쿼터 조정 불필요). |
| 2026-08-05 | 2.1.0 | fix | 신규 84건(dev MFT 29 + INV/DIR 36 + holdout MFT 12 + F-R3 신규 7) | #333 Part 2 라운드2 F-R4 — `createdAt`이 전부 `2026-08-02`(v1 원 저작일)로 잘못 찍혀 있던 것을 실제 저작일 `2026-08-05`로 정정. v1 재기록 43건(labeler-01)의 `createdAt`은 원 저작일 그대로 유지(변경 없음) — 구매 이력 기준일 `2026-08-02`(GUIDE `orderedAt` 규약)와는 무관한 별개 필드다. |
| 2026-08-05 | 2.1.0 | docs | GUIDE.md·manifest.json | #333 Part 2 라운드2 판정 회신 §7-1/§7-2/§7-4 반영. §7-1: 카테고리 계층 표기 관측(2026-08-05 실측)과 `mustExcludeProductIds` 워크어라운드를 GUIDE에 명문(validator는 이번 라운드에서 바꾸지 않음 — 계약 판단은 후속 이슈). §7-2: `manifest.sliceQuotas.holdout.confirmatorySlicesMin`을 `requiredSlicesMinEach`로 개명 — dev 쪽 `confirmatory.confirmatorySlices`(N≥30 라벨링, guest/member/budget)와는 별개 개념(`audit.py`의 `required_holdout` 슬라이스별 최소 목표)임을 GUIDE에 명확화, holdout `failure` 5/6은 dev confirmatory 사전 등록 위반이 아님을 승인. §7-4: 하드 네거티브 실측 혼합비(`semantic_near` 90.07% / `random_catalog` 9.93%, `attr_violation`·`price_violation`·`other_brand` 0%에 가까움)와 사유(대다수 케이스가 `category` 미지정이라 카테고리 전제 채널이 트리거되지 않음)를 GUIDE에 명문 — 데이터가 정본이라 조작하지 않는다. |
| 2026-08-05 | 2.1.0 | relabel | 전 케이스(127건) | 라운드2 반영 후 manifest.json 재계산(counts/파일 해시/datasetHash), `run_audit(write=True)` 재실행. |
| 2026-08-06 | 2.1.0 | relabel | dev 35건(buy-budg-0002/0004/0006/0008/0010, buy-cmap-0001/0002, buy-cold-0001/0002, buy-mult-0001/0002/0003/0006/0007/0008, buy-over-0001/0002, buy-pers-0002/0003/0004/0006/0007/0008/0009, buy-repu-0001/0004/0007/0008, buy-srch-0002/0003/0008/0009/0010/0011/0013) | 독립 검수자 adjudicator-omx-01(127건 전수 검수, dispute 45건)의 dev dispute 35건을 catalog 근거 재검증 후 반영 — 성별/브랜드/재구매 등급 오류 정정(예: 여성 청바지 질의의 남성 전용 상품 제거, 재구매 시나리오의 타 브랜드 relevant 제거), 암묵 0(경계) 누락 보강(injected 후보 승격 포함, `injected-relevant-approved:` 마커). `buy-pers-0004`는 인용 2건 중 1건(productId 3700228373)이 이미 relevant로 라벨돼 있어 사실관계 오류로 판단해 그 부분만 반려, 나머지 1건은 반영(부분 반려). 등급≥1 비율 상한 초과 케이스는 `relevant-ratio-exempt:` 마커로 예외 처리(정답을 임의로 누락하지 않음). |
| 2026-08-06 | 2.1.0 | relabel | holdout 10건(sealed — `buyer_holdout_labels.jsonl`만 변경, 라벨 값은 봉인 관례상 명기하지 않음) | 위와 동일한 adjudication 반영을 holdout dispute 10건에 적용. `buy-cmap-1002`는 반영 시 `buy-cold-0002`(dev)와 정답 집합이 과도하게 겹쳐(`relevantSetOverlap` 0.875, 상한 0.5 초과) `run_audit` 위반이 새로 발생함을 확인 — 케이스의 실제 취지(카테고리 오분류 견고성 테스트)에 맞춰 명백히 오분류된 후보로만 반영 범위를 좁혀 위반을 해소(0 violation). |
| 2026-08-06 | 2.1.0 | docs | manifest.json·GUIDE.md·README.md | adjudicator-omx-01을 `manifest.adjudicators`에 추가하고 전 127건에 `adjudicator: "adjudicator-omx-01"` 기록. `manifest.adjudicationSummary`에 검수 통계(전수 340 라벨 + 표본 820, dispute 45건 → 반영 44/부분반려 1, agreement rate (340-37)/340≈0.891) 신설(#329 권고 8 1차 이행). GUIDE.md relevant-ratio 절에 "상한은 후보 구성 규약이지 명백한 정답을 0으로 만드는 허가가 아니다" 명문 추가, 알려진 한계 절 신설(member_recall_ge_guest 자명성·신라면/커피믹스 cross-split 재사용·price=null injected 후보 — 데이터 변경 없음, 후속 이슈). README.md의 정체된 "120건(dev 96)"을 실제 "127건(dev 103)"으로 동기화. manifest.json 재계산(파일 해시/datasetHash), `run_audit(write=True)` 재실행(0 violation). |
