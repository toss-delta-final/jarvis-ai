"""판매자 트리거 판정 검증 하네스 (이슈 #595, `12-EVAL` §6).

현재 검증층(F1~F3 · D1~D3 · C1~C4 · judge)에는 **정답 대조가 0건**이다 — 전부 형식과
어휘를 재고, judge 는 LLM 이 LLM 을 채점한다. "이 판정이 맞았나"를 재는 검사가 없다.
이 패키지가 그 자리를 셋으로 채운다.

- ``synth``     : 이상 0건·요일 효과만 있는 합성 브랜드(집계 응답 수준) 생성 — 결정론
- ``null_sim``  : 1,000일 발동률 측정 → **배포 전 필수 게이트**(결정 119)
- ``goldenset`` : 알려진 이상을 심고 잡는지 보는 회귀 케이스 9종(결정 120)
- ``ari``       : 군집 안정성 Adjusted Rand Index(결정 121)

[왜 원시 이벤트가 아니라 집계 응답 수준을 합성하나 — 사용자 확정]
`data-analysis/generate_dummy.py` 는 원시 행(behavior_events·orders)을 만든다. 그것을
트리거에 먹이려면 Spring 집계 SQL(I-6/I-7/I-13/I-8/I-14)을 파이썬으로 재구현해야 하는데,
이슈 #595 가 판정 함수를 스케줄러보다 먼저 만들게 한 이유(**이중 구현 금지**)가 한 층
아래에서 그대로 재발한다. 게다가 실측상 그 생성기는 1,000일에 못 쓴다 — 볼륨이
`period_days` 에 비례하지 않고(1,000일이면 40건/일), 주문 상태가 `period_end` 까지의
거리 함수라 추세가 주입되며, checkout 이탈 예산이 시간순으로 소진돼 계단 변화가 생긴다.

그래서 **집계 응답 수준**을 직접 합성한다. 요일 효과의 앵커만 그 생성기와 같은 원천
(`data-analysis/stats_jarvis.json`, REES46 2019-10 실측)에서 가져온다.

[하네스 규약 — `evals/README.md` 계승]
- 지표는 분자·분모 정의를 동봉한다(`null_sim.NullSimReport`).
- 데이터 버전이 바뀌면 baseline 을 재실행한다(`manifest.json` 의 `datasetVersion`).
- 결정론이라 CI 에서 돈다(`@pytest.mark.eval`) — 확률적 하네스가 아니다.
"""
