-- 미사용 order_seed 테이블을 pg-catalog 에서 제거한다(이슈 #124).
--
-- 주문 미러/시드 노선은 2026-07-15 에 기각됐다 — 구매 이력은 질의 시점 I-19 조회
-- (api-spec §4.7), 판매자 통계는 집계 콜백(§4.4)으로 대체됐고 이 테이블을 읽는 코드는
-- 없다. 스키마도 상품·주문 원본 사본이라 "AI Postgres 에는 AI 생성물만 저장" 규칙에
-- 어긋난다(이슈 #65 의 products 원본 컬럼 제거와 같은 취지).
--
-- init 스크립트(01_order_seed.sql) 삭제는 새로 만드는 볼륨에만 효과가 있으므로,
-- 이미 테이블이 생성된 기존 볼륨을 위해 이 migration 을 수동 1회 적용한다.
-- 기존 볼륨에 반복 적용해도 안전하다.

BEGIN;

DROP TABLE IF EXISTS order_seed;

COMMIT;
